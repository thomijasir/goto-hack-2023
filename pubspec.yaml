name: goto_hack_2023
description: 'A new Flutter project.'
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.9
  sizer: ^2.0.15
  asphalt_aloha:
    git:
      url: git@source.golabs.io:asphalt/asphalt-aloha-flutter.git
      path: asphalt_aloha
      ref: 2.2.0
  draggable_customized_btn_navy_bar: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^3.0.1
flutter:
  uses-material-design: true

  assets:
    - assets/
 